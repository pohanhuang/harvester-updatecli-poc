sources:
  golang:
    kind: golang
    name: golang versions
    # {{ if .source.pattern }}
    spec:
      versionfilter:
        kind: semver
        pattern: '{{ .source.pattern }}'
    # {{ end }}
